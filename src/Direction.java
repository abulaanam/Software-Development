/**
 * This enum represents the possible directions that a link can have in the rail network.
 * Each direction is represented by a unique name. Additional directions can be added as needed.
 */
public enum Direction {
        one,   // represents direction one
        two,   // represents direction two
        three, // represents direction three
        four;   // represents direction four
}